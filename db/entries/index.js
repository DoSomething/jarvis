'use strict';

// Add *all* new entries to this file.
//
// If your code has entry dependencies,
// simply require them all with `require(`${global.entries}`)`

require('./Entry');
require('./Keyword');
